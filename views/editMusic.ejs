<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Songtify</title>
    <style>
        body {
            background-image: url('/images/walpaper.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
    </style>
</head>

<body>
    <%- include('partials/navbar') %>

        <div class="container">
            <br>
            <div class="text-center">
                <h2>Update Music</h2>
            </div>
            <br>
            <div class="text-center">
                <% if (music) { %>
                    <form action="/editMusic/<%= music.musicId %>" method="POST" enctype="multipart/form-data">
                        <label for="title">Title:</label><br>
                        <input type="text" id="title" name="title" value=<%=music.title %> required><br><br>

                        <label for="artist">Artist:</label><br>
                        <input type="text" id="artist" name="artist" value=<%=music.artist %> required><br><br>

                        <label for="releaseDate">Release Date:</label><br>
                        <input type="date" id="releaseDate" name="releaseDate" min="0" step="0.01"
                            value=<%=music.releaseDate %> required><br><br>

                        <label for="image">Current Image:</label><br>
                        <input type="text" name="currentImage" value=<%=music.images %> readonly ><br>
                        <img src="/images/<%= music.image%>" width="20%"><br><br>
                        New Image:<br><input type="file" id="image" name="image" accept="image/*" required><br><br>

                        <button type="submit">Update Music</button>
                    </form>
                    <% } else { %>
                        <p>No Music found.</p>
                        <% } %>
                            <a href="/">Back</a>
            </div>
        </div>
</body>

</html>